:3001 {
	root * /srv
	encode gzip

	# Health endpoint — must be before the SPA fallback
	handle /health {
		respond "OK" 200
	}

	# SPA fallback — serve static files, fall back to index.html
	handle {
		try_files {path} /index.html
		file_server
	}
}
